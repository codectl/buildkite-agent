---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: privileged-buildkite-agent
rules:
  - apiGroups: [ policy ]
    resources: [ podsecuritypolicies ]
    verbs: [ use ]
    resourceNames: [ privileged-buildkite-agent ]
  - apiGroups: [ "" ]
    resources: [ pods, pods/log, deployments, replicasets, statefulsets, configmaps, secrets ]
    verbs: [ create, get, list, update, delete, patch, watch ]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: buildkite-agent
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: privileged-buildkite-agent
subjects:
  - kind: ServiceAccount
    name: buildkite-agent