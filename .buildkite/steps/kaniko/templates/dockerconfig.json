{
  "auths": {
    "${REGISTRY}": {
      "auth": "${CREDENTIALS}"
    }
  },
  "proxies": {
    "default": {
      "httpProxy": "${HTTP_PROXY}",
      "httpsProxy": "${HTTPS_PROXY}",
      "noProxy": "${NO_PROXY}"
    }
  }
}
